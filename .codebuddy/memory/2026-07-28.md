# 2026-07-28

## chore：完成中断的清理改动并修复 QueryQueue 死代码
- 工作树遗留一批未提交的清理改动（identifier 改为 com.smartlab.desktop、i18n 联系作者文案改为 Unique、移除未使用变量/导入等）。
- `QueryQueue.tsx` 之前仅把 `handleDragStart/handleDragOver` 加 `_` 前缀但仍是死代码（JSX 用内联重复逻辑）。改为真正接回 JSX（`onDragStart={(e) => handleDragStart(e, index)}` / `onDragOver={(e) => handleDragOver(e, index)}`），消除重复。
- `npx tsc --noEmit` 已通过，无 TS 错误。
- commit 524a399。
