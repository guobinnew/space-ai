# 2026-07-28

## 计算机操作界面复刻 + 授权应用功能
- 复刻 smart-code 的 ComputerUseSettings 界面，增加授权应用管理功能
- **win_helper.py**: 新增 `list_installed_apps` action，支持两种输入格式（JSON 字符串 / 纯命令字符串），从 Windows 注册表（HKLM/HKCU Uninstall）读取已安装应用
- **computerUseService.ts**: 重构 `getStatus()` 返回详细结构（python/venv/dependencies/permissions），`runSetup()` 返回步骤化结果（SetupStep[]），新增 `listInstalledApps()`、`loadConfig()`/`saveConfig()`（存储到 `~/.spaceai/computer-use-config.json`）、`openSystemSettings()`
- **computerUse.ts API**: 新增 `GET /apps`、`GET/PUT /authorized-apps`、`POST /open-settings` 路由
- **features.ts**: 扩展类型（ComputerUseStatus 新结构、SetupStep、InstalledApp、AuthorizedApp、ComputerUseConfig），API 方法改名 `status()`→`getStatus()`、`setup()`→`runSetup()`
- **ComputerUseSettings.tsx**: 完全重写——状态检测卡片（StatusRow/StatusIcon）、步骤化安装结果、授权应用列表（搜索+自动保存+排序）、Grant Flags 开关（剪贴板/系统快捷键）。使用内联 SVG 图标替代 smart-code 的 material-symbols
- **i18n**: 新增 30+ 翻译 key（中英文）
- commit ec90978

### Bug 修复：授权应用列表为空
- **根因**：用户之前运行过 setup，部署了旧版 win_helper.py（不含 `list_installed_apps` action）到 `~/.spaceai/computer-use-runtime/`。代码更新后未重新运行 setup，runtime 目录中的脚本过时
- **修复**：`listInstalledApps()` 中先调用 `deployHelperScript()` 确保部署最新脚本，再执行命令。增加错误日志输出
- commit b23a29f

### 服务商预设增加 Kimi
- 前端 `desktop/src/config/providerPresets.ts` 新增 Kimi 预设（之前缺失）
- 服务端 `server/agent/config/providerPresets.ts` 更新 Kimi 为官方文档配置
- Kimi 配置：baseUrl `https://api.moonshot.cn/v1`，apiFormat `openai`，默认模型 `kimi-k3`，支持图片输入
- commit 9bf4db7

### Kimi (OpenAI 格式) 空 assistant content 修复
- 问题：LLM 仅返回工具调用时 `fullText=''`，保存到历史后 content 为空字符串，Kimi API 拒绝
- 修复：runOpenAILoop 中 history→messages 映射时，assistant 消息空 content 转为 null
- commit ab21769

### LLM 服务全预设适配审查 + 修复
- 审查结论：URL 构建、认证头、工具定义映射、工具结果格式、流式解析、compact 均已正确适配两种 API 格式（anthropic: DeepSeek/Zhipu GLM；openai: Kimi/Qwen/Mimo）
- 修复 1：OpenAI 循环补充 `newMessages` 处理（SkillTool 注入技能内容，Anthropic 循环已有，OpenAI 循环缺失）
- 修复 2：`callOpenAI` 的 `stream_options.include_usage` 兼容性降级（400 时自动重试不带 stream_options）
- commit 584888f

### TTS 朗读模式禁用 + 定时任务会话同步
- CodeEditor.tsx：挂载时通过 providersApi.list() 检查激活服务商 models.tts，未配置时禁用朗读按钮，tooltip 提示原因
- Sidebar.tsx：增加 15s 轮询 fetchSessions，定时任务新建的会话自动显示
- 修复 ReadingPanel.tsx useTTS 导入路径错误（预先存在）
- commit ecc8911
