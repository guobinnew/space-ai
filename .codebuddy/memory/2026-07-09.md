# 2026-07-09

## 文件编辑器照搬 smart-code 实现 (commit 1435534)

会话页面的文件编辑器从简化版（只读 FileViewer）升级为完整 Monaco 编辑器实现，照搬 `D:\Work\CodePlan\smart-code\desktop\src\components\editor` 全部 10 个组件。

### 新增桌面端文件
- `components/editor/` — 10 个组件：CodeEditor、FileExplorer、EditorPanel、ImagePreview、DocxPreview、XlsxPreview、PptxPreview、useJitViewer、monacoLoader、EditorErrorBoundary
- `stores/editorStore.ts` — zustand store，管理打开文件/Git状态/本地diff计算（LCS算法）
- `stores/pendingRefStore.ts` — zustand store，编辑器→聊天的文件/代码引用传递
- `components/shared/Tooltip.tsx` + `Modal.tsx` — 共享组件
- `api/git.ts` — Git status/diff API 客户端

### 修改的桌面端文件
- `api/client.ts` — 添加 `getBaseUrl()` 导出
- `api/filesystem.ts` — 重写为 browse/read/write/delete/mkdir/move 接口
- `pages/ActiveSession.tsx` — 改用新 `EditorPanel`（无 props，读 editorStore），workDir 变化时调用 `setExplorerRoot`
- `main.tsx` — 导入 `monacoLoader`（side-effect import）
- `i18n/index.ts` — 添加 editor.* 和 fileExplorer.* 翻译键
- `theme/globals.css` — 添加 Material Symbols 字体、hide-scrollbar、glass-panel、git gutter 装饰样式

### 删除的文件
- `components/filesystem/` — 旧简化版 EditorPanel/FileExplorer/FileViewer

### 服务端变更
- `api/filesystem.ts` — 重写，添加 browse/file/write/delete/mkdir/move 端点（含路径安全检查 isAllowedFilesystemPath）
- `api/git.ts` — 新建，git status --porcelain + diff
- `router.ts` — 添加 git 路由
- `middleware/errorHandler.ts` — 添加 `ApiError.forbidden`

### 依赖
- monaco-editor@0.55.1、@monaco-editor/react@4.7.0、zustand@5.0.13、@tauri-apps/plugin-opener@2.5.4、jit-viewer@1.5.0

### 适配点（与 smart-code 的差异）
- CodeEditor 用 `useUIStore` 替代 `useSettingsStore`（取 locale）
- CodeEditor/FileExplorer 用 `usePendingRefStore` 替代 `useChatStore`（setPendingCodeRef/setPendingFileRef/setPendingDirRef）
- editorStore 用 `console.error` 替代 `useUIStore.addToast`（SpaceAI uiStore 无 addToast）
- MarkdownRenderer 调用简化（去掉 variant/basePath props，SpaceAI 版本不支持）
- monacoLoader 去掉 `@ts-expect-error`（SpaceAI 的 vite-env.d.ts 已有 worker 类型声明）

## Assistant 消息动画头像 + 思考消息显示 (commit b07ce31)

### 前端变更
- `AssistantMessage.tsx`: 添加 AgentAvatar SVG 机器人头像（眨眼动画 `animate-blink`），布局改为头像+气泡水平排列
- `ThinkingBlock.tsx`: 重写为支持实际思考内容（content prop），折叠时显示首行预览，展开时窗口化渲染（15k window），memo+流式节流
- `StreamingIndicator.tsx`: 改为药丸形状 + shimmer 动画（✦ 图标）
- `MessageList.tsx`: 传递 thinkingText 给 ThinkingBlock
- `chatStore.tsx`: 添加 thinkingText 状态，处理 thinking_delta WS 消息
- `types/chat.ts`: PerSessionChatState 添加 thinkingText
- `websocket.ts`: ServerMessage 添加 thinking_delta
- `globals.css`: 添加 animate-blink/animate-shimmer/animate-pulse-dot

### 服务端变更
- `llmStreamService.ts`:
  - StreamChunk 添加 `thinking_delta` 类型
  - AnthropicContentBlock 添加 `thinking` 类型（含 signature）
  - Anthropic API 请求启用 extended thinking（`thinking: { type: 'enabled', budget_tokens: 10000 }`）
  - max_tokens 从 8192 提升至 16000
  - 流式解析器处理 thinking_delta/signature_delta/content_block_stop
  - LLMResponse 返回 thinkingBlocks，agentic loop 将 thinking 块加入对话历史（Anthropic API 要求）

