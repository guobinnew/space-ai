# 2026-06-22

## smart-space 项目初始化

- 创建了 smart-space monorepo 项目，npm workspaces 管理
- **desktop/**: Vite + React 18 + Tauri v2 桌面客户端
- **server/**: TypeScript + Node.js + Express 服务端，esbuild 打包为单文件 `dist/server.js`
- Tauri 打包时将 `server/dist/server.js` 作为 resource 嵌入
- `lib.rs` 在 `setup` 阶段以子进程方式启动 Node.js 服务，应用关闭时自动 kill
- 开发模式下 fallback 到 `../server/dist/server.js` 路径
- 服务端端口 3721，监听 127.0.0.1
- Cargo 镜像源从 tuna 切换到 rsproxy（tuna 不可达）
- 图标为脚本生成的占位 PNG，后续可用 `npx tauri icon` 替换
- Git 初始提交在 master 分支
- 添加 `tauri:dev` 命令：先构建服务端，再用 concurrently 并行启动服务端和 Tauri 客户端
- 修复：`tauri:dev` 添加 `--kill-others` 标志，关闭窗口时自动终止所有进程
- 重构子进程管理：Tauri 自动启动/停止 server 子进程，Windows 用 `taskkill /T /F` 杀进程树，`on_window_event` 监听窗口关闭
- `tauri:dev` 简化为只构建服务端 + 启动 Tauri（不再用 concurrently 单独启动服务端）
- 修复 dev 路径：`current_dir()` 在 dev 模式下是 `desktop/src-tauri`，需要 `.parent().parent()` 才能到项目根目录找到 `server/dist/server.js`
- 生成高质量图标：SVG 源文件（深蓝渐变背景 + 六边形网络节点设计），用 sharp 转换为各尺寸 PNG + 标准 ICO 格式（含 16/24/32/48/64/128/256 多分辨率）
- 添加闪屏功能：双窗口方案（splash + main），splash 显示加载动画，前端就绪后调用 `close_splashscreen` 命令关闭闪屏并显示主窗口

## 参照 smart-code 实现主界面布局

- 参考项目 `D:\Work\CodePlan\smart-code`，在 desktop 中实现类 AppShell 主界面布局
- 引入 Tailwind CSS v4（`tailwindcss` + `@tailwindcss/vite`），vite.config.ts 加 tailwindcss 插件
- 新增 `src/theme/globals.css`：移植 smart-code 设计令牌（light/dark 双主题 via `data-theme`，CSS 变量 `--color-*`/`--radius-*`/`--shadow-*`，sidebar 动画类）
- 布局组件 `src/components/layout/`：AppShell(根 h-screen flex)、Sidebar(可折叠侧边栏+导航项+主题切换)、TabBar(标签栏+可关闭)、WindowControls(Windows 自定义最小化/最大化/关闭)、ContentRouter(按 activeTab 渲染页面)
- 状态管理 `src/stores/uiStore.tsx`：React Context 实现（sidebarOpen/theme/tabs/activeTabId），未引入 zustand
- 页面 `src/pages/`：HomePage(保留服务器状态检查 /api/health + /api/info，新设计)、SettingsPage(主题切换占位)
- 删除旧 `App.css`；App.tsx 改为 `<UIProvider><AppShell/></UIProvider>`
- 主窗口 tauri.conf.json 设 `decorations:false` + minWidth/minHeight，capabilities 加窗口权限(allow-minimize/toggle-maximize/is-maximized/close/start-dragging)
- 图标全用内联 SVG，不依赖 Material Symbols 字体；字体用系统字体栈
- 修复预先存在的 tsconfig.node.json 问题：被 tsconfig.json references 但缺 `composite:true` 且 noEmit，导致 TS6306/TS6310；改为 composite:true 移除 noEmit
- 默认 dark 主题，主题持久化到 localStorage('smartspace-theme')，main.tsx 渲染前初始化 data-theme 防闪烁

## 修复 splash 窗口关闭误杀 server 的 bug

- 现象：应用启动后 server 进程立即被 kill（日志显示 Server started 后紧接 Killing server process）
- 根因：`lib.rs` 的 `on_window_event` 监听所有窗口的 `WindowEvent::Destroyed`，未区分窗口 label；前端 HomePage 延迟 500ms 调 `close_splashscreen` 关闭 splash 窗口 → 触发 Destroyed → 误执行 `state.kill()` 杀掉 server
- 修复：`on_window_event` 加 `window.label() == "main"` 判断，只在主窗口销毁时 kill server

## 设置页改造：分类导航 + 主题切换迁移

- 设置页 `SettingsPage.tsx` 重写为左侧分类导航(180px: 通用/关于) + 右侧内容区布局，参考 smart-code Settings.tsx
- "通用"分类下放主题切换，用分段按钮(浅色/深色)样式，参考 smart-code GeneralSettings
- uiStore 新增 `setTheme(t)` 方法（直接指定主题，区别于 toggleTheme）
- Sidebar 底部移除主题切换按钮及 SunIcon/MoonIcon，仅保留设置导航项
