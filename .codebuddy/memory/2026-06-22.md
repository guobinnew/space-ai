# 2026-06-22

## smart-space 项目初始化

- 创建了 smart-space monorepo 项目，npm workspaces 管理
- **desktop/**: Vite + React 18 + Tauri v2 桌面客户端
- **server/**: TypeScript + Node.js + Express 服务端，esbuild 打包为单文件 `dist/server.js`
- Tauri 打包时将 `server/dist/server.js` 作为 resource 嵌入
- `lib.rs` 在 `setup` 阶段以子进程方式启动 Node.js 服务，应用关闭时自动 kill
- 开发模式下 fallback 到 `../server/dist/server.js` 路径
- 服务端端口 3721，监听 127.0.0.1
- Cargo 镜像源从 tuna 切换到 rsproxy（tuna 不可达）
- 图标为脚本生成的占位 PNG，后续可用 `npx tauri icon` 替换
- Git 初始提交在 master 分支
- 添加 `tauri:dev` 命令：先构建服务端，再用 concurrently 并行启动服务端和 Tauri 客户端
- 修复：`tauri:dev` 添加 `--kill-others` 标志，关闭窗口时自动终止所有进程
- 重构子进程管理：Tauri 自动启动/停止 server 子进程，Windows 用 `taskkill /T /F` 杀进程树，`on_window_event` 监听窗口关闭
- `tauri:dev` 简化为只构建服务端 + 启动 Tauri（不再用 concurrently 单独启动服务端）
- 修复 dev 路径：`current_dir()` 在 dev 模式下是 `desktop/src-tauri`，需要 `.parent().parent()` 才能到项目根目录找到 `server/dist/server.js`
- 生成高质量图标：SVG 源文件（深蓝渐变背景 + 六边形网络节点设计），用 sharp 转换为各尺寸 PNG + 标准 ICO 格式（含 16/24/32/48/64/128/256 多分辨率）
- 添加闪屏功能：双窗口方案（splash + main），splash 显示加载动画，前端就绪后调用 `close_splashscreen` 命令关闭闪屏并显示主窗口

## 参照 smart-code 实现主界面布局

- 参考项目 `D:\Work\CodePlan\smart-code`，在 desktop 中实现类 AppShell 主界面布局
- 引入 Tailwind CSS v4（`tailwindcss` + `@tailwindcss/vite`），vite.config.ts 加 tailwindcss 插件
- 新增 `src/theme/globals.css`：移植 smart-code 设计令牌（light/dark 双主题 via `data-theme`，CSS 变量 `--color-*`/`--radius-*`/`--shadow-*`，sidebar 动画类）
- 布局组件 `src/components/layout/`：AppShell(根 h-screen flex)、Sidebar(可折叠侧边栏+导航项+主题切换)、TabBar(标签栏+可关闭)、WindowControls(Windows 自定义最小化/最大化/关闭)、ContentRouter(按 activeTab 渲染页面)
- 状态管理 `src/stores/uiStore.tsx`：React Context 实现（sidebarOpen/theme/tabs/activeTabId），未引入 zustand
- 页面 `src/pages/`：HomePage(保留服务器状态检查 /api/health + /api/info，新设计)、SettingsPage(主题切换占位)
- 删除旧 `App.css`；App.tsx 改为 `<UIProvider><AppShell/></UIProvider>`
- 主窗口 tauri.conf.json 设 `decorations:false` + minWidth/minHeight，capabilities 加窗口权限(allow-minimize/toggle-maximize/is-maximized/close/start-dragging)
- 图标全用内联 SVG，不依赖 Material Symbols 字体；字体用系统字体栈
- 修复预先存在的 tsconfig.node.json 问题：被 tsconfig.json references 但缺 `composite:true` 且 noEmit，导致 TS6306/TS6310；改为 composite:true 移除 noEmit
- 默认 dark 主题，主题持久化到 localStorage('smartspace-theme')，main.tsx 渲染前初始化 data-theme 防闪烁

## 修复 splash 窗口关闭误杀 server 的 bug

- 现象：应用启动后 server 进程立即被 kill（日志显示 Server started 后紧接 Killing server process）
- 根因：`lib.rs` 的 `on_window_event` 监听所有窗口的 `WindowEvent::Destroyed`，未区分窗口 label；前端 HomePage 延迟 500ms 调 `close_splashscreen` 关闭 splash 窗口 → 触发 Destroyed → 误执行 `state.kill()` 杀掉 server
- 修复：`on_window_event` 加 `window.label() == "main"` 判断，只在主窗口销毁时 kill server

## 设置页改造：分类导航 + 主题切换迁移

- 设置页 `SettingsPage.tsx` 重写为左侧分类导航(180px: 通用/关于) + 右侧内容区布局，参考 smart-code Settings.tsx
- "通用"分类下放主题切换，用分段按钮(浅色/深色)样式，参考 smart-code GeneralSettings
- uiStore 新增 `setTheme(t)` 方法（直接指定主题，区别于 toggleTheme）
- Sidebar 底部移除主题切换按钮及 SunIcon/MoonIcon，仅保留设置导航项

## 复刻 smart-code server 到 server/agent + sidecar 模式

- 将 smart-code `src/server` 架构复刻到 `server/agent/`，改用 **Bun.serve** 运行时（smart-code 原始用 Bun.serve）
- server/agent 目录结构: `server.ts`(Bun.serve 核心)、`index.ts`(导出 startServer)、`router.ts`(API路由)、`sidecar.ts`(统一入口)、`middleware/`(cors/auth/errorHandler)、`api/`(status/sessions桩)、`ws/handler.ts`(WebSocket桩)、`types.ts`
- sidecar.ts 参照 smart-code `desktop/sidecars/smart-sidecar.ts`，支持 `server` 模式 + `--app-root` launcher 参数解析
- API: /api/health、/api/info（兼容前端）、/api/status、/api/sessions(桩)；WebSocket /ws/* /sdk/*
- server/agent/package.json: `bun run sidecar.ts server` 启动，`bun build --compile` 打包为独立二进制
- desktop lib.rs 重写: dev 模式用 `bun run sidecar.ts server`，生产模式用编译后的二进制；通过 `is_compiled` 判断
- tauri.conf.json resources 改为 `../../server/dist/agent/` → `agent/`
- 根 package.json 添加 server/agent workspace
- 修复: router.ts import 路径应为 `./api/status`(同级) 非 `../api/status`；Bun 自动解析无扩展名 .ts import
- 验证: bun 启动 sidecar 后 /api/health 和 /api/info 正常返回；cargo check 通过

## 修复 Windows 上 Command::new("bun") 找不到 bun 的问题

- 现象: tauri:dev 启动报 `Failed to start sidecar process: Error { kind: NotFound, message: "program not found" }`
- 根因: Windows 上 bun 通过 npm 安装为 shim(bun.cmd)，Rust 的 `Command::new("bun")` 底层用 CreateProcess，不解析 .cmd 扩展名
- 修复: dev 模式 Windows 上用 `cmd /C bun run sidecar.ts ...` 包装，cmd.exe 通过 PATHEXT 找到 bun.cmd；非 Windows 仍直接用 `bun`

## 设置页增加服务商分类 + LLM Provider CRUD

- 参照 smart-code 的 provider 系统复刻
- 后端 server/agent: `types/provider.ts`、`config/providerPresets.ts`(DeepSeek/Zhipu/Kimi/Qwen/Custom)、`services/providerService.ts`(JSON存储 ~/.smartspace/providers.json)、`api/providers.ts`(CRUD+activate+test)；router.ts 加 providers 路由
- 前端 desktop: `types/provider.ts`、`config/providerPresets.ts`、`api/client.ts`(fetch封装)、`api/providers.ts`、`components/settings/ProviderSettings.tsx`(列表+增删改查+测试+激活+表单Modal)
- SettingsPage 增加"服务商"分类导航项(通用/服务商/关于)
- Provider 存储: ~/.smartspace/providers.json (activeId + providers[])；激活时写入 ~/.smartspace/settings.json 的 env
- API Key 从服务端返回时做 mask 处理(前4+****+后4)

## 实现会话管理功能和会话页面

- 参照 smart-code 会话管理架构复刻（简化版）
- 后端 server/agent: `types/session.ts`、`services/sessionService.ts`(JSON文件存储 ~/.spaceai/sessions/)、`api/sessions.ts`(CRUD+messages)；sessions API 重写为完整实现
- 前端 desktop: `types/session.ts`、`api/sessions.ts`、`stores/sessionStore.tsx`(Context: sessions/messages/fetch/create/delete/rename/sendMessage)
- 页面: `pages/ActiveSession.tsx`(消息列表+输入框+重命名+删除)、`pages/EmptySession.tsx`(空会话欢迎页+输入创建)
- uiStore TabType 增加 'session'；ContentRouter 路由 session 标签 → ActiveSession
- Sidebar 增加新会话按钮+会话列表展示；TabBar 增加 ChatGlyph 图标
- App.tsx 包裹 SessionProvider
- api/client.ts 增加 patch 方法
- 会话存储: ~/.spaceai/sessions/<id>.json (每会话一文件) + index.json (索引)
- 消息发送: 乐观更新(先加user消息) → 保存到服务端 → 添加assistant占位回复

## 实现 WS 流式 LLM 会话

- 参照 smart-code WS 协议和 chat 组件架构复刻（简化版）
- 后端: `services/llmStreamService.ts`(用活跃provider配置调LLM API流式返回，支持Anthropic/OpenAI)；`server.ts` WebSocket handler 实现 user_message→streamChat→流式回推(content_start/content_delta/status/message_complete/error)
- 前端: `api/websocket.ts`(WSManager单例，per-session连接+心跳+重连)、`stores/chatStore.tsx`(per-session流式状态: messages/chatState/streamingText)、`types/chat.ts`(UIMessage/ChatState/PerSessionChatState)
- chat 组件: `components/chat/MessageList.tsx`(消息列表+流式渲染)、`UserMessage.tsx`、`AssistantMessage.tsx`(含光标)、`StreamingIndicator.tsx`(三点动画)、`ChatInput.tsx`(自动高度+停止生成)
- ActiveSession 重写为 chatStore 驱动(connectToSession→WS连接→sendMessage→流式接收)
- EmptySession 改用 chatStore(connectToSession + sendMessage via WS)
- App.tsx 添加 ChatProvider
- WS 协议: 客户端发 user_message/ping/stop_generation；服务端回 connected/content_start/content_delta/status/message_complete/error/pong

## 重构 sidecar 架构：Server sidecar(单例) + CLI sidecar(per-session)

- 参照 smart-code 架构：Server sidecar(lib.rs setup 启动，整个应用生命周期唯一) + CLI sidecar(conversationService 每次会话新建子进程)
- lib.rs: `ServerSidecar(Mutex<Option<Child>>)` 单例；setup 阶段自动启动(sidecar.ts server --port 3721)；主窗口关闭时 kill；invoke_handler 只保留 close_splashscreen + get_server_port
- conversationService.ts: `startSession`/`sendMessage`/`stopSession`/`endSession`；每会话 SessionProcess(activeStream/outputCallback)；WS open 时 startSession，close 时 endSession
- server.ts WS handler: open→conversationService.startSession(callback=ws.send)；message user_message→conversationService.sendMessage；close→conversationService.endSession
- chatStore: 移除 invoke('start_session_sidecar')，改为直接连接固定端口 3721；disconnectSession 仅断开 WS（Server 自动清理 CLI sidecar）
- 固定端口 3721；Server sidecar 内部 conversationService 管理每会话的 LLM streaming
